meta {
  name: Atualizar plano
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/planos/{{planoTesteId}}
  body: json
  auth: inherit
}

body:json {
  {
    "descricao": "Descrição atualizada via Bruno"
  }
}
